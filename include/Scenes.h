#ifndef __SCENES_H__
#define __SCENES_H__

/**
 * Esse arquivo serve apenas para definir as cenas.
 * Cada cena seria relativo a um trabalho, tipo um main para cada trabalho porém usando o mesmo core.
 */

#include <entities/Entity.h>

Entity *generateScene1();
Entity *generateScene2();
Entity *generateScene3();

#endif